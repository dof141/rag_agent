历史会话：
{history_text}

当前用户问题：{query}

你是一个强大的知识库实体提炼与问题重写专家。请根据历史会话和当前问题完成以下两件事：

1. 提取核心主题/课程/软件名称（item_names）：
   - 请从问题中提取出用户真正想询问的核心软件、课程、技术或资料名称（如 "Neo4j 5.26"、"MySQL"、"尚硅谷大模型技术"）。
   - 只要问题中包含具体的软件名、技术名或课程名，必须提取出来放在 item_names 中！绝对不能返回空列表！
   - 如果使用了代词（如"这个"、"它"），请结合历史会话指代消解后提取。
   - 只有在打招呼、闲聊或彻底无任何具体技术/课程主体时，item_names 才返回 []。

2. 改写问题（rewritten_query）：
   - 重新改写用户的问题，使其成为包含完整主体的独立问题。
请严格返回 JSON 格式，不要包含任何解释性文字或多余格式：
{{
    "item_names": ["核心技术或课程名称"],
    "rewritten_query": "补全主体名称后的完整独立问题？"
}}